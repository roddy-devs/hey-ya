Initialize the Django backend for this project using Poetry.
Follow SPEC.md and copilot-instructions.md.

Requirements:
- Django + Django REST Framework
- PostgreSQL support
- Django built-in auth
- No virtualenv usage (Poetry only)
- Prepare models for User, Session, Hole
- No frontend integration yet
- No cloud-specific config yet
